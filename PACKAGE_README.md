# Vue Auto I18n - 快速安装包

## 📦 安装包文件

**vue-auto-i18n-2.0.0.tgz** (82KB)

一个真正的单一安装包，包含所有功能，一条命令即可安装！

---

## 🚀 超级简单的安装方式

### 第一步：复制文件

把 `vue-auto-i18n-2.0.0.tgz` 复制到你的项目根目录

### 第二步：一条命令安装

```bash
pnpm add ./vue-auto-i18n-2.0.0.tgz
```

或使用其他包管理器：

```bash
npm install ./vue-auto-i18n-2.0.0.tgz
yarn add ./vue-auto-i18n-2.0.0.tgz
bun add ./vue-auto-i18n-2.0.0.tgz
```

### 第三步：开始使用

安装后立即可以使用，无需额外配置

---

## 📖 使用示例

### Vite 配置

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoI18n from 'vue-auto-i18n'

export default defineConfig({
  plugins: [
    vue(),
    AutoI18n({
      translator: { type: 'google', apiKey: 'xxx' },
      locales: ['en', 'ja', 'ko']
    })
  ]
})
```

### CLI 工具

```bash
vue-auto-i18n extract --translate
```

---

## ✨ 一次安装，包含所有功能

- ✅ Vite 插件
- ✅ 核心库
- ✅ 代码替换工具
- ✅ CLI 命令行工具
- ✅ TypeScript 类型

---

## 📚 详细说明

查看 [安装说明.md](安装说明.md) 获取完整安装指南

---

## 💡 为什么选择这个包？

- **一条命令安装** - 无需复杂的配置
- **包含所有功能** - 不需要单独安装子包
- **开箱即用** - 安装后立即可以使用
- **完整支持** - 包含 Vite 插件、CLI、核心库等全部功能
